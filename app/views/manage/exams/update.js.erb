<% if @notification %>
  $("#exam-list").html("<%= j(render partial: 'exams', locals: { exams: @exams }) %>");
  $('#exam-model').modal('hide');
  $('.modal-backdrop').hide();
  pushNotify('<%= @notification[:status] %>', '<%= @notification[:title] %>', '<%= @notification[:message] %>');
<% end %>
