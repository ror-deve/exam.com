<% if @exams.present? %>
  $("#exam-list").html("<%= j render 'manage/exams/exams', exams: @exams %>");
<% else %>
  $("#exam-list").html("<p class='result-not-found'>Exam not found with '<%= params[:query]%>'");
<% end %>
