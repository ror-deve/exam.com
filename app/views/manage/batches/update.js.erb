<% if @notification %>
  $("#batch-list").html("<%= j(render partial: 'batches', locals: { batches: @batches }) %>");
  $('#batch-model').modal('hide');
  $('.modal-backdrop').hide();
  pushNotify('<%= @notification[:status] %>', '<%= @notification[:title] %>', '<%= @notification[:message] %>');
<% end %>
